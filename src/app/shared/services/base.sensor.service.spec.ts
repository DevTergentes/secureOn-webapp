import { BaseSensorService } from './base.sensor.service';

describe('BaseSensorService', () => {
  it('should create an instance', () => {
    expect(new BaseSensorService()).toBeTruthy();
  });
});
